class CheckPrivacyPolicyAction {
  final bool playload;
  CheckPrivacyPolicyAction(this.playload);
}

class CheckTermsConditionsAction {
  final bool playload;
  CheckTermsConditionsAction(this.playload);
}

class CreateAnAccountAction {
  final bool playload;
  CreateAnAccountAction(this.playload);
}

class Loading {
  final bool playload;
  Loading(this.playload);
}

class ShowPassword {
  final bool playload;
  ShowPassword(this.playload);
}

class ShowPasswordRepeat {
  final bool playload;
  ShowPasswordRepeat(this.playload);
}
