class SignInToAccountAction {
  final bool playload;
  SignInToAccountAction(this.playload);
}

class Loading {
  final bool playload;
  Loading(this.playload);
}

class ShowPassword {
  final bool playload;
  ShowPassword(this.playload);
}
